# ============================================================
# Front-end (spark.js / Vite renderer)
# ============================================================
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Scan assets — too large for git; shared separately (zip / Drive).
# Keep the SmallRoom collider GLB (4.8 MB) so a fresh clone runs.
public/splats/*
!public/splats/README.md
public/models/*
!public/models/MGstudio_SmallRoom.glb

# Dead scaffolding kept on disk only (couldn't be deleted from the sandbox)
SparkTest.js
SparkTest/
src/counter.js
src/assets/
src/xr.js
test-spark.html
test-spark-cdn.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============================================================
# Back-end (Python brain)
# ============================================================
__pycache__/
*.pyc
.env
.venv/
venv/
.pytest_cache/

# Recorded Qwen responses are cache artifacts; keep the dir, ignore contents.
fixtures/*
!fixtures/.gitkeep

# Generated agent run logs.
agent_log.jsonl
*_bboxes.png

# Real captured photos — personal & large; the placeholder PNGs suffice for tests.
photos/*.jpeg
photos/*.jpg
photos/*.HEIC
photos/*.heic

# Session transcript (build log, not code).
sample_json_first_perspective/claude_session_1.txt

# ============================================================
# Shared
# ============================================================
.DS_Store
